# SearchChips 组件字典参考

本文档是 `figma-component-dictionary` 的组件族 reference，只服务 `componentFamily = SearchChips`（笔记 搜索标签）。

## 适用记录

- `SearchChips_01`（笔记 业务专用）

## 真实属性键

| 字段 | 说明 | 已验证值 |
| --- | --- | --- |
| `variantid` | 主变体 | `_01` |

## 落位规则

- 用于搜索激活态的快速筛选标签
- 通常落于 `SearchReceiving_*` 面板内部

## 缺口

- `nodeId` / `componentKey` / `componentSetId` 待补
- 内部 padding / 自然尺寸待 探查

> CSV1 / CSV2 同步日期：2026-05-18。
