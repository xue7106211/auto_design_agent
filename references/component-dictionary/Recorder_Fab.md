# Recorder_Fab 组件字典参考

本文档是 `figma-component-dictionary.md` 的组件族 reference，只服务 `componentFamily = Recorder_Fab`（业务_录音 FAB）。

## 适用记录

命中以下 `variantId` 时加载本文档：

- `Fab_Rec_01`（业务_录音 浮动按钮）

## 真实属性键

| 字段 | 说明 | 已验证值 |
| --- | --- | --- |
| `variantid` | 主变体 | `Fab_Rec_01` |

## 落位规则

- 用于「录音」应用的浮动操作按钮，独立于通用 `Fab` family
- 通用 Fab spec 参见 `Fab.md`

## 缺口

- `nodeId` / `componentKey` / `componentSetKey` 待 Figma 探查后补齐
- 内部 padding / size 待补

> CSV2 同步日期：2026-05-19。从通用 Fab family 切分独立。
