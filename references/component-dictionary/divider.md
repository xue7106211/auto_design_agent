# Divider 组件字典参考

本文档是 `figma-component-dictionary` 的组件族 reference，只服务 `componentFamily = Divider`（分割线）。

## 适用记录

- `Divider_ComponentSet_01`

## 真实属性键

| 字段 | 说明 | 已验证值 |
| --- | --- | --- |
| `variantid` | 主变体 | `_01` |

## 内部 padding（按 CSV2）

- 左 28；右 28

## 落位规则

- 通常作为 List item 之间的分隔
- 颜色通过 token `分割线色/outline` 绑定（key `96f2cf4d1ce0d56cff2f8e98da6a5e16bd59983e`）

## 缺口

- `nodeId` / `componentKey` / `componentSetId` 待补

> CSV1 / CSV2 同步日期：2026-05-18。
