---
name: figma-adapt-nlc-layout
description: 在现有 Figma 目标 frame 内完成 NLC（导航-列表-内容）三栏布局适配。适用于"把有底部 Tab 的应用适配为折叠屏三栏""侧边导航加列表加详情""在目标 frame 里做三栏布局"等场景。由主 Skill figma-multi-terminal-adapt 委托调用，也可独立使用。
disable-model-invocation: false
---

# NLC 三栏布局适配

在目标 frame 内完成 NLC（导航-列表-内容）三栏布局适配：左侧导航栏 + 中间列表栏 + 右侧内容区。手机端底部 Tab 转为侧边导航栏。不新建并行页面，直接执行。

## 适用场景

- "把有底部 Tab 的应用适配为折叠屏三栏"
- "侧边导航 + 列表 + 详情"
- "在这个 frame 里做 NLC 三栏布局"
- "底部 Tab 转侧边导航栏"

## 前置条件

执行前需确认以下信息已就绪（由主 Skill 传入或自行获取）：

- 源设计稿节点 ID 和结构摘要
- 目标设备类型（Fold / Pad）
- 目标画布尺寸和三栏栏宽（参考 `references/device-dimensions.md`）
- 目标 frame 已存在且有编辑权限
- 已识别源页面的底部 Tab 导航组件

## 核心原则

1. 先探查，后修改
2. 优先复用已有组件、变体、画布节点（详见 `references/common-rules.md`）
3. 能 clone 已落地节点时，不优先 create
4. 所有写入分步执行，不要一次性大脚本
5. 每一步写入后做截图校验和结构校验
6. 实例化失败时立即切换 clone 策略
7. 底部 Tab 到侧边导航的转换是 NLC 的核心操作，必须确保导航项完整迁移

## 强制工作流

### Phase A：搭目标骨架

读取布局规则：`references/layout-nlc.md`

执行：
- 清空目标 frame 子节点
- 设置目标 frame 尺寸（从 `references/device-dimensions.md` 获取）
- 建立全局状态栏（通过 `search_design_system` 搜索目标设备变体，或 clone 源页面状态栏）
- 建立主内容区 frame（水平布局）
- 建立导航栏 frame（最左列，宽度按 `references/layout-nlc.md` 定义）
- 建立列表栏 frame（中间列，宽度按 `references/layout-nlc.md` 定义）
- 建立内容区 frame（最右列，宽度按 `references/layout-nlc.md` 定义）
- 为视口容器设置 `clipsContent = true`

写入模式参考：`references/plugin-api-patterns.md`

完成后校验：截图 + 确认三栏骨架尺寸正确。

### Phase B：填充导航栏

读取组件规则：`references/component-routing.md` + `references/component-adaptation.md`

执行：
- 搜索侧边导航栏组件（优先 `search_design_system`，其次查 `references/component-routing.md`）
- 如果有现成的侧边导航变体，直接实例化
- 如果没有，从源页面底部 Tab 提取导航项信息（图标、文字、数量），构建侧边导航
- 确保导航项数量和顺序与源页面底部 Tab 一致
- 设置当前选中态（与列表栏内容对应的导航项）
- 删除源页面的底部 Tab 导航

完成后校验：截图 + 确认导航栏宽度、导航项完整性。

### Phase C：填充列表栏

执行：
- 放入源页面顶部模块（搜索栏、标题栏等，应用属性切换规则）
- 替换或放入目标列表变体
- 应用组件属性切换（参考 `references/component-adaptation.md`）
- 让列表填充顶部模块以下的剩余视口
- 删除不需要的移动端底部元素

完成后校验：截图 + 确认列表栏宽度和内容正确。

### Phase D：填充内容区

执行：
- 放入详情导航栏
- 建立正文视口 frame
- 放入正文内容（clone 源详情页内容区）
- 固定底部操作区（如有）
- 删除移动端状态栏、底部手势条、键盘弹层等
- 通过视口裁切表达当前可见区域

完成后校验：截图 + 确认内容区宽度和内容正确。

### Phase E：整体调整

- 检查三栏间距 / 分割线
- 检查边距是否符合 `references/layout-nlc.md` 定义
- 确认全局状态栏只有一套
- 确认导航栏选中项 → 列表栏内容 → 内容区详情三者语义一致

完成后校验：全页截图 + 完整结构校验。

## 每步校验标准

### 截图校验

每次关键写入后调用 screenshot，检查：
- 是否有重叠
- 是否有裁切错误
- 是否有异常留白
- 三栏宽度是否视觉正确
- 导航栏图标和文字是否清晰
- 顶部状态栏是否只有一套

### 结构校验

每次关键写入后读取结构，检查：
- 目标 frame 尺寸
- 状态栏尺寸
- 三栏各自尺寸
- 导航项数量
- 顶层节点数量和位置

## 修正原则

结果不符合预期时：
1. 先修尺寸
2. 再修位置
3. 最后修文本或局部视觉

不要整页重做，只做局部修正。

## 输出要求

最终只输出：
- 已完成的结构变化
- 实际尺寸结果（导航栏宽度、列表栏宽度、内容区宽度、总宽度）
- 导航项迁移情况（数量、是否完整）
- 是否满足验收项
- 是否存在妥协项

## 默认验收标准

- 目标页面尺寸精确匹配设备规格
- 三栏宽度精确匹配 `references/layout-nlc.md` 定义
- 顶部全局状态栏只有一套
- 导航栏项数和顺序与源页面底部 Tab 一致
- 导航栏选中态正确
- 列表栏不保留移动端底部导航语义
- 内容区保留正文与操作区，不保留键盘弹层
- 导航选中项、列表内容、详情内容三者语义一致
- 整体视觉无明显裁切、重叠、异常空白
